{
  "name": "personal-budget-app",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "node scripts/validate-env.js && tsc && vite build",
    "preview": "vite preview",
    "setup:env": "node scripts/setup-env.js",
    "validate:env": "node scripts/validate-env.js",
    "deploy:firebase": "npm run validate:env && npm run build && firebase deploy",
    "deploy:netlify": "npm run validate:env && npm run build && netlify deploy --prod --dir=dist",
    "deploy:vercel": "npm run validate:env && vercel --prod",
    "deploy:github": "npm run validate:env && npm run build && gh-pages -d dist"
  },
  "dependencies": {
    "date-fns": "^2.30.0",
    "firebase": "^12.0.0",
    "lucide-react": "^0.294.0",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-firebase-hooks": "^5.1.1",
    "react-router-dom": "^6.20.1",
    "recharts": "^2.8.0"
  },
  "devDependencies": {
    "@types/react": "^18.2.37",
    "@types/react-dom": "^18.2.15",
    "@vitejs/plugin-react": "^4.1.1",
    "autoprefixer": "^10.4.16",
    "gh-pages": "^6.3.0",
    "netlify-cli": "^22.3.0",
    "postcss": "^8.4.32",
    "tailwindcss": "^3.3.6",
    "terser": "^5.43.1",
    "typescript": "^5.2.2",
    "vite": "^7.0.5"
  },
  "keywords": [
    "budget",
    "finance",
    "react",
    "typescript",
    "tailwindcss"
  ],
  "author": "Your Name",
  "license": "MIT"
}
